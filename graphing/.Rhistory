for (i in 1:iterations){
r_new = 1 + 1/(r_curr)
ratios = c(ratios, r_new)
r_curr = r_new
}
return(ratios)
}
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],10)
output = paste0(first_10, '...'last_10))
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
# functions
generate_ratios = function(all_r_0){
all_ratios = list()
for (i in 1:length(all_r_0)){
all_ratios[[i]] = iterate(all_r_0[i], iterations)
}
return(all_ratios)
}
iterate = function(r_0, iterations) {
ratios = c(r_0)
r_curr = r_0
for (i in 1:iterations){
r_new = 1 + 1/(r_curr)
ratios = c(ratios, r_new)
r_curr = r_new
}
return(ratios)
}
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],10)
output = paste0(first_10, '...',last_10))
first_10 = head(all_ratios[[i]],10)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],10)
output = paste0(first_10,'...',last_10))
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],10)
output = paste0(first_10,'...',last_10)
print(output)
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
ratios[[1]]
test = head(ratios[[1]], 10)
test
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],10)
print(first_10)
print(last_10)
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
iterate = function(r_0, iterations) {
ratios = c(r_0)
r_curr = r_0
for (i in 1:iterations){
r_new = 1 + 1/(r_curr)
ratios = c(ratios, r_new)
r_curr = r_new
}
return(ratios)
}
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],1)
print(first_10)
print(last_10)
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],1)
print(first_10, last_10)
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
tail(ratios[[i]],1)
tail(ratios[[1]],1)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],1)
print(paste(first_10, last_10, sep = '...'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],2)
print(paste(first_10, last_10, sep = '...'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_10 = tail(all_ratios[[i]],2)
cat(paste(first_10, last_10, sep = '...'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_2 = tail(all_ratios[[i]],2)
output = c(first_10, last_2)
cat(output)
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_2 = tail(all_ratios[[i]],2)
output = c(first_10, last_2)
cat(output\n)
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_2 = tail(all_ratios[[i]],2)
output = c(first_10, last_2)
cat(paste0(first_10))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
last_2 = tail(all_ratios[[i]],2)
output = c(first_10, last_2)
cat(paste0(first_10,'\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results = function(all_r_0, all_ratios) {
print_results = function(all_r_0, all_ratios) {
}
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0(first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
}
)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0(first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
# functions
generate_ratios = function(all_r_0){
all_ratios = list()
for (i in 1:length(all_r_0)){
all_ratios[[i]] = iterate(all_r_0[i], iterations)
}
return(all_ratios)
}
iterate = function(r_0, iterations) {
ratios = c(r_0)
r_curr = r_0
for (i in 1:iterations){
r_new = 1 + 1/(r_curr)
ratios = c(ratios, sprintf("%.3f", r_new))
r_curr = r_new
}
return(ratios)
}
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0(first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ': ', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ': \t', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ': \t\t', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
cat(paste0('r=', all_r_0[i], ':', first_10,'...', last_2, '\n'))
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ':', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = ", ")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ':\t', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ':\t', first_10,'...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = ", ")
cat(paste0('r=', all_r_0[i], ':\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r=', all_r_0[i], ':\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r=', all_r_0[i], ':\t\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r_0=', all_r_0[i], ':\t\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r_0 = ', all_r_0[i], ':\t\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r_0 = ', all_r_0[i], ':\t\t', first_10,'\t...', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0('r_0 = ', all_r_0[i], ':\t\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
cat('\thi')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
cat('\thi\n')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
View(ratios)
print_results = function(all_r_0, all_ratios) {
cat('r_0\tr_1\tr_2\tr_3\tr_4\tr_5\tr_6\tr_7\tr_8\tr_9\tr_{10}\t...\tr_{100}\tr_{101}')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
cat('r_0\tr_1\tr_2\tr_3\tr_4\tr_5\tr_6\tr_7\tr_8\tr_9\tr_{10}\t...\tr_{100}\tr_{101}\n')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
print_results = function(all_r_0, all_ratios) {
cat('r_{0}\tr_{1}\tr_2\tr_{3}\tr_{4}\tr_5\tr_{6}\tr_{7}\tr_{8}\tr_{9}\tr_{10}\t...\tr_{100}\tr_{101}\n')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
# parameters
iterations = 49
starting_ratios = c(-1, -0.5, 1, 2)
# functions
generate_ratios = function(all_r_0){
all_ratios = list()
for (i in 1:length(all_r_0)){
all_ratios[[i]] = iterate(all_r_0[i], iterations)
}
return(all_ratios)
}
iterate = function(r_0, iterations) {
ratios = c(r_0)
r_curr = r_0
for (i in 1:iterations){
r_new = 1 + 1/(r_curr)
ratios = c(ratios, sprintf("%.3f", r_new))
r_curr = r_new
}
return(ratios)
}
print_results = function(all_r_0, all_ratios) {
cat('r_{0}\tr_{1}\tr_2\tr_{3}\tr_{4}\tr_5\tr_{6}\tr_{7}\tr_{8}\tr_{9}\tr_{10}\t...\tr_{49}\tr_{50}\n')
for (i in 1:length(all_ratios)){
first_10 = head(all_ratios[[i]],10)
first_10 <- paste0(first_10, collapse = "\t")
last_2 = tail(all_ratios[[i]],2)
last_2 <- paste0(last_2, collapse = "\t")
cat(paste0(all_r_0[i], '\t', first_10,'\t...\t', last_2, '\n'))
}
}
# generate ratios for all r_0
ratios = generate_ratios(starting_ratios)
print_results(starting_ratios, ratios)
View(ratios)
version
install.packages("installr")
library(installr)
updateR()
updateR()
updateR()
version
setwd("~/Documents/Research/scripts/graphing/")
